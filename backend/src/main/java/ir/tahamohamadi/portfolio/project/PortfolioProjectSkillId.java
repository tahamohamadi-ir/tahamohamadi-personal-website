package ir.tahamohamadi.portfolio.project; import jakarta.persistence.*; import lombok.*; import java.io.Serializable; import java.util.*; @Embeddable @Getter @NoArgsConstructor(access=AccessLevel.PROTECTED) public class PortfolioProjectSkillId implements Serializable { @Column(name="portfolio_project_id") private UUID projectId; @Column(name="skill_id") private UUID skillId; public PortfolioProjectSkillId(UUID projectId,UUID skillId){this.projectId=projectId;this.skillId=skillId;} @Override public boolean equals(Object o){return this==o||(o instanceof PortfolioProjectSkillId i&&Objects.equals(projectId,i.projectId)&&Objects.equals(skillId,i.skillId));} @Override public int hashCode(){return Objects.hash(projectId,skillId);} }
