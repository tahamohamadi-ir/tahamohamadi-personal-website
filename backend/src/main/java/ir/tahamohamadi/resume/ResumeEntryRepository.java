package ir.tahamohamadi.resume; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ResumeEntryRepository extends JpaRepository<ResumeEntry,UUID>{ Page<ResumeEntry> findByDeletedAtIsNull(Pageable page); }
