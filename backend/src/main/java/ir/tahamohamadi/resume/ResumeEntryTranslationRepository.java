package ir.tahamohamadi.resume; import ir.tahamohamadi.common.domain.*; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.*; import org.springframework.data.repository.query.Param; import java.util.*; public interface ResumeEntryTranslationRepository extends JpaRepository<ResumeEntryTranslation,UUID>{ List<ResumeEntryTranslation> findByEntryIdAndDeletedAtIsNull(UUID entryId); List<ResumeEntryTranslation> findByEntryIdInAndDeletedAtIsNull(Collection<UUID> entryIds); @Query(value="select t from ResumeEntryTranslation t join fetch t.entry e where t.languageCode=:language and t.deletedAt is null and e.deletedAt is null and e.status='PUBLISHED'",countQuery="select count(t) from ResumeEntryTranslation t join t.entry e where t.languageCode=:language and t.deletedAt is null and e.deletedAt is null and e.status='PUBLISHED'") Page<ResumeEntryTranslation> findPublishedByLanguage(@Param("language") LanguageCode language,Pageable page); }
