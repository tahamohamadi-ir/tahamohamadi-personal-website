package ir.tahamohamadi.resume; import ir.tahamohamadi.common.domain.*; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ResumeDocumentRepository extends JpaRepository<ResumeDocument,UUID>{ Optional<ResumeDocument> findByLanguageCodeAndStatusAndDeletedAtIsNull(LanguageCode language,ContentStatus status); Optional<ResumeDocument> findByLanguageCodeAndDeletedAtIsNull(LanguageCode language); Page<ResumeDocument> findByDeletedAtIsNull(Pageable page); }
