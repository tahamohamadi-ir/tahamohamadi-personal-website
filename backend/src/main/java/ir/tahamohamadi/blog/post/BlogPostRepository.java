package ir.tahamohamadi.blog.post; import org.springframework.data.domain.*; import org.springframework.data.jpa.repository.*; import java.util.*; public interface BlogPostRepository extends JpaRepository<BlogPost,UUID>{ @EntityGraph(attributePaths="category") Page<BlogPost> findByDeletedAtIsNullOrderByUpdatedAtDescIdDesc(Pageable pageable); }
