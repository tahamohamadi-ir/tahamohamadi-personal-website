# Frontend design-system compatibility redirect

The former specification is now the canonical [UI/UX documentation suite](../ui-ux/README.md). Its complete 27-set legacy inventory and mapping are retained in [design-system governance](../ui-ux/design-system/README.md).

Runtime token values are authoritative only in [`frontend/src/css/tokens.scss`](../../frontend/src/css/tokens.scss). This redirect contains no normative rules or executable values.
